# 🧠 System Architecture

## Flow

Frontend → Backend API → Game Engine → Wallet Service → MongoDB → WebSocket

## Services

- frontend (React UI)
- backend (API Gateway)
- game-engine (RNG logic)
- wallet-service (balance system)
- auth-service (JWT login)
